/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TecPipe } from './tec.pipe';

describe('Pipe: Tec', () => {
  it('create an instance', () => {
    let pipe = new TecPipe();
    expect(pipe).toBeTruthy();
  });
});
